# 🎯 在哪里运行脚本？

## 环境说明

你现在有两个环境：

### 1️⃣ **本地开发环境**（你现在所在的地方）
- **位置**：你的电脑 / Claude Code 环境 / 当前终端
- **路径**：`/root/projects/Agent-Skills/`
- **用途**：编写代码、运行开发工具、配置 SSH 连接

### 2️⃣ **远程 Strapi 服务器**
- **位置**：云服务器 / VPS / 生产服务器
- **访问方式**：通过 SSH 连接
- **用途**：运行 Strapi 应用、数据库、Web 服务

## 📜 脚本运行位置指南

### ✅ 在 **本地** 运行的脚本：

```bash
# 1. 设置脚本（配置 SSH 连接）
./strapi-remote-setup.sh

# 2. 开发辅助脚本
./strapi-dev.sh connect   # 连接到远程
./strapi-dev.sh tunnel    # 建立 SSH 隧道
```

**为什么在本地运行？**
- 需要配置本地的 SSH 客户端
- 需要设置本地的端口转发
- 需要创建本地的快捷命令

### ✅ 在 **远程服务器** 运行的命令：

```bash
# 需要先 SSH 连接到远程服务器
ssh your-server

# 然后在远程服务器上运行：
cd /path/to/strapi
npm install
npm run develop
pm2 start ecosystem.config.js
```

## 🚀 完整操作流程

### Step 1: 在本地配置（当前位置）

```bash
# 你现在在本地 Claude Code 环境
pwd  # 应该显示 /root/projects/Agent-Skills

# 运行设置脚本
./strapi-remote-setup.sh
```

运行后，脚本会要求你输入：
- 远程服务器 IP 地址
- SSH 用户名
- SSH 端口
- 远程 Strapi 项目路径

### Step 2: 脚本自动创建的文件（都在本地）

```
本地机器:
├── ~/.ssh/config          # SSH 配置（本地）
├── ./strapi-dev.sh       # 快捷命令脚本（本地）
└── ./strapi-remote-setup.sh  # 设置脚本（本地）
```

### Step 3: 使用配置连接到远程

```bash
# 方式 1：直接 SSH
ssh strapi-remote

# 方式 2：使用创建的脚本
./strapi-dev.sh connect

# 方式 3：VSCode Remote-SSH
# 在 VSCode 中连接到 strapi-remote
```

## 📝 实际例子

假设你的远程服务器信息是：
- IP: 192.168.1.100
- 用户: ubuntu
- Strapi 路径: /home/ubuntu/my-strapi

### 1. 在本地运行设置脚本

```bash
# 当前在本地
$ ./strapi-remote-setup.sh

=== Strapi 远程服务器设置向导 ===

请输入服务器 IP 或域名: 192.168.1.100
请输入 SSH 端口 (默认 22): 22
请输入用户名: ubuntu
请输入 Strapi 项目路径: /home/ubuntu/my-strapi
```

### 2. 脚本创建本地 SSH 配置

```bash
# 自动写入 ~/.ssh/config
Host strapi-remote
    HostName 192.168.1.100
    Port 22
    User ubuntu
    LocalForward 1337 localhost:1337
```

### 3. 使用配置连接

```bash
# 现在可以简单地连接
ssh strapi-remote

# 连接后，你就在远程服务器上了
ubuntu@remote-server:~$ cd /home/ubuntu/my-strapi
ubuntu@remote-server:~/my-strapi$ npm run develop
```

## ⚠️ 常见误解

### ❌ 错误做法：
```bash
# 不要这样做 - 在远程服务器上运行本地设置脚本
ssh your-server
./strapi-remote-setup.sh  # 错误！这个脚本应该在本地运行
```

### ✅ 正确做法：
```bash
# 在本地运行设置脚本
./strapi-remote-setup.sh  # 正确！在本地配置 SSH

# 然后连接到远程
ssh strapi-remote

# 在远程运行 Strapi 命令
cd /your/strapi/path
npm run develop
```

## 🎯 总结

| 操作 | 在哪里运行 | 原因 |
|------|-----------|------|
| `strapi-remote-setup.sh` | **本地** | 配置本地 SSH 客户端 |
| `strapi-dev.sh` | **本地** | 本地快捷命令 |
| SSH 配置 | **本地** | ~/.ssh/config 是本地文件 |
| `npm run develop` | **远程** | Strapi 在远程运行 |
| `pm2 start` | **远程** | 管理远程进程 |
| VSCode Remote-SSH | **本地启动** | 从本地连接到远程 |

**记住**：设置脚本是你的"遥控器"，在本地配置好后，用它来控制远程服务器！